package com.dnod.simplemovie.utils;

/**
 * This is a place where we will register our constants values(f.e. Loader's id's, ActivityForResult request codes, etc...)
 */
public interface Constants {
    int MOVIES_LOADER_ID = 1;
}
